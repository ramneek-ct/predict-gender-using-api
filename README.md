A simple PHP project that predicts the gender of a name using the Genderize.io API.

The user enters a name, and the application calls the API to determine the most likely gender along with the probability.
